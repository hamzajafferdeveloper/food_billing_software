import { Button } from './ui/button';

export const LoadMoreBtn = () => {
    return (
        <div className="flex items-center justify-center">
            <Button type="button" variant="outline" className="btn btn-primary">
                Load more
            </Button>
        </div>
    );
};
